# Mimir Meta Repository

This repository is a meta project that aggregates multiple components as Git submodules:
- `AuthServer`
- `Gateway`
- `MimirSAM`

## Updating submodules
To update this project and its submodules, run:

```bash
git submodule update --init --recursive --remote
```
